# Doelenboom: lokale stack starten/herstarten met "doelenboom -local -restart"
# (zie scripts/doelenboom-cli.sh voor wat dit precies doet en welke andere
# vlaggen er zijn — dit is bewust alleen nog maar een dunne alias ernaartoe,
# geen losse eigen implementatie meer, om te voorkomen dat de twee uit de pas
# gaan lopen zoals eerder gebeurd is).
alias doelenboom="$HOME/OneDrive/src/doelenboom/scripts/doelenboom-cli.sh"
